import React from 'react'
import styled from 'styled-components'

const Social = ({ className }) => {
	return <div className={className}>Social</div>
}

export default styled(Social)`
	border: 2px solid pink;
`
